using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;

public class MenuGameName : MonoBehaviour
{
    private TextMeshPro textMeshPro;

    // Start is called before the first frame update
    void Start()
    {
        //textMeshPro = gameObject.GetComponent<TextMeshPro>(); // ?? gameObject.AddComponent<TextMeshPro>();

        //Debug.Log(textMeshPro);
        //var totalVisibleCharacters = textMeshPro.textInfo.characterCount;
        //int counter = 0;
        //while(counter < totalVisibleCharacters)
        //{
        //    var visibleCount = counter++ % (totalVisibleCharacters + 1);
        //    textMeshPro.maxVisibleCharacters = visibleCount;
        //    new WaitForSeconds(0.5f);
        //}

        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
